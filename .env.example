# Optional build-time override. The default API/web host is defined once, in
# config/env.ts (ENV.API_BASE_URL) — leave this unset unless overriding it.
# API_BASE_URL=
GOOGLE_WEB_CLIENT_ID=your-web-client-id-here
